<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.repository.ArticleLikeRepository">
    <select id="getLikeCount" parameterType="int" resultType="int">
        SELECT COUNT(*) FROM article_like WHERE article_Id = #{articleId}
    </select>
    <select id="isLiked" resultType="int">
        SELECT COUNT(*) FROM article_like
        WHERE article_Id = #{articleId} AND member_Id = #{memberId}
    </select>
    <insert id="insertLike" parameterType="map">
        INSERT INTO article_like (article_Id, member_Id, reg_Date)
        VALUES (#{articleId}, #{memberId}, NOW())
    </insert>
    <delete id="deleteLike" parameterType="map">
        DELETE FROM article_like
        WHERE article_Id = #{articleId} AND member_Id = #{memberId}
    </delete>
</mapper>